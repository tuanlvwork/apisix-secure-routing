apiVersion: v1
kind: Service
metadata:
  name: apisix-admin
  namespace: gateway
  labels:
    app: apisix-admin
spec:
  selector:
    app: apisix-admin
  ports:
    - name: admin
      port: 9180
      targetPort: 9180
  type: ClusterIP   # Overridden to NodePort in local overlay
