apiVersion: apps/v1
kind: Deployment
metadata:
  name: apisix-admin
  namespace: gateway
  labels:
    app: apisix-admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apisix-admin
  template:
    metadata:
      labels:
        app: apisix-admin
    spec:
      initContainers:
        # Wait for etcd before starting APISIX control plane
        - name: wait-for-etcd
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z etcd.gateway.svc.cluster.local 2379; do
                echo "Waiting for etcd..."; sleep 2
              done
              echo "etcd is up"
      containers:
        - name: apisix-admin
          image: apache/apisix:3.15.0-debian
          ports:
            - name: admin
              containerPort: 9180   # Admin API + Admin UI (/ui)
          volumeMounts:
            - name: apisix-admin-config
              mountPath: /usr/local/apisix/conf/config.yaml
              subPath: config.yaml
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /apisix/admin/routes
              port: 9180
              httpHeaders:
                - name: X-API-KEY
                  value: supersecretadminkey
            initialDelaySeconds: 15
            periodSeconds: 5
            failureThreshold: 12
          livenessProbe:
            tcpSocket:
              port: 9180
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: apisix-admin-config
          configMap:
            name: apisix-admin-config
