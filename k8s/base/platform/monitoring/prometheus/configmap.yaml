apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: gateway
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s

    scrape_configs:
      - job_name: 'apisix-external'
        static_configs:
          - targets: ['apisix-external.gateway.svc.cluster.local:9091']
        metrics_path: '/apisix/prometheus/metrics'

      - job_name: 'apisix-internal'
        static_configs:
          - targets: ['apisix-internal.gateway.svc.cluster.local:9091']
        metrics_path: '/apisix/prometheus/metrics'
