apiVersion: v1
kind: Service
metadata:
  name: etcd-internal
  namespace: gateway
  labels:
    app: etcd-internal
spec:
  selector:
    app: etcd-internal
  ports:
    - name: client
      port: 2379
      targetPort: 2379
    - name: peer
      port: 2380
      targetPort: 2380
  type: ClusterIP
