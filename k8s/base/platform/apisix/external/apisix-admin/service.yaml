apiVersion: v1
kind: Service
metadata:
  name: apisix-admin-external
  namespace: gateway
  labels:
    app: apisix-admin-external
spec:
  selector:
    app: apisix-admin-external
  ports:
    - name: admin
      port: 9180
      targetPort: 9180
  type: ClusterIP   # Overridden to NodePort in local overlay
